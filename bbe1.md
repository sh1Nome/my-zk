---
title: bash のプロセス置換について
tags: [bash]
---

* `<()`
    * コマンドの標準出力を読み込み用ファイルとして扱う
* `>()`
    * コマンドの標準入力を書き込み用ファイルとして扱う
* `diff <(コマンドA) <(コマンドB)`
    * コマンドAとコマンドBの出力を比較する
* `コマンドA > >(tee out.txt) 2> >(tee err.txt)`
    * 標準出力と標準エラー出力を別のファイルに書き込みながら画面へ表示する
