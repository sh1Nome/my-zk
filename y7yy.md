---
title: Windows のコマンドライン
tags: [windows]
---

## 概要

cmd と powershell について、とりあえずこれだけ分かればなんとかなる。

## cmd

* `(command) /h`
    * コマンドのヘルプを表示する
* `where (command)`
    * コマンドのパスを表示する

## PowerShell

* `help (command)`
    * コマンドのヘルプを表示する
* `Get-Command (command)`
    * コマンドのパスを表示する
    * （`where.exe (command)`）でもOK

