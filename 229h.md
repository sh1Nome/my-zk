---
title: xargs の使い方
tags: [command, bash, linux]
---

`(コマンドA) | xargs (コマンドB)` でコマンドAの実行結果を複数まとめて引数に渡し、コマンドBを実行する。  
`-n1` オプションを使うことでコマンド A の実行結果を 1 つずつ引数に私、都度コマンド B を実行する。  
`-n3` の場合は 3 つずつ渡す。

```
$ ls | xargs echo
aaa bbb ccc ddd eee
$ ls | xargs -n3 echo
aaa bbb ccc
ddd eee
```

`-I` オプションで引数を 1 つずつ任意の位置に挿入できる。  

```
$ ls | xargs -I {} echo "file: {}"
file: aaa
file: bbb
file: ccc
file: ddd
file: eee
```

