---
title: プロセスとポートの紐づけの確認方法
tags: [linux, command]
---

## ポート番号からプロセスを特定する方法

* `lsof -Pan -i :(ポート番号)`
    * `-P`: ポート番号を数値で表示、`-a`: AND条件、`-n`: ホスト名を数値で表示、`-i`: ネットワークファイルを選択

## プロセスからポート番号を特定する方法

* `lsof -Pan -p (プロセス番号) -i`
    * `-P`: ポート番号を数値で表示、`-a`: AND条件、`-n`: ホスト名を数値で表示、`-p`: プロセスIDを指定、`-i`: ネットワークファイルを選択
