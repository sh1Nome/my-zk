---
title: zk の使い方
tags: [zk, cli]
---

zk はツェッテルカステンをするための CLI

* `zk init` でノートブックを作る
* `zk new --title "タイトル"` でノートを作る
* `zk list` でノート一覧を見る
* `zk list --tag foo` でタグごとにノートを見る
* `zk tag list` でタグ一覧を見る
