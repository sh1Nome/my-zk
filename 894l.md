---
title: mise の tasks の注意点
tags: [mise, tool, cli]
---

mise の tasks は Windows だと扱いが難しい。  
実行環境は `windows_default_inline_shell_args` で設定できる。  
今回は MSYS2 の UCRT 版 bash から mise を利用しているが、  
この設定を `sh -c -o errexit` にするとうまく動かなかった。  
原因はおそらく次の2点。  

1. MSYS2 のランチャーを経由せず sh.exe を直接実行するため、環境がおかしくなる  
2. `sh -c` の引数がクオートで囲まれず、コマンドを正しく受け取れていない  

(ただしこの原因分析は mise や msys2 の実装コードを確認したものではなく、  
動作結果からの推測であり、正確でない可能性がある)  

このあたりは深追いせず、デフォルトの `cmd /c` のまま使うのが無難そうだった。  
MSYS2 から実行すると PATH は引き継がれるため、  
構文さえ cmd 準拠であればコマンド自体は動く。  
ただし MSYS2 の環境変数を引き継いだ cmd は、  
問題が起きたときに原因の切り分けが面倒になりそうという懸念がある。  

結局、MSYS2 の環境に閉じて作業するなら make を使ったほうが楽そう、という結論に至った。  
mise はツールのインストール用途に留めるのが良さそう。  

環境: mise 2026.6.11 windows-x64 (2026-06-16)
