---
title: ssh で接続先を保存する方法
tags: [linux, ssh]
---

`~/.ssh/config` に以下の設定を書くと `ssh (Host名)` で接続できる。

```
Host example.com
    Hostname example.com
    User example
    Port 22
    IdentityFile /path/to/identityfile
```

