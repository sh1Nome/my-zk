---
title: ファイル／ディレクトリの作成／移動／削除
tags: [linux, command]
---

* `mkdir (ディレクトリ名)`
    * ディレクトリを作成する
* `touch (ファイル名)`
    * ファイルを作成する
* `cp (パス1) (パス2)`
    * ファイルをパス1からパス2にコピーする
* `cp -r (パス1) (パス2)`
    * ディレクトリをパス1からパス2にコピーする
* `mv (パス1) (パス2)`
    * パス1をパス2に移動する
* `rm -rf (ファイル名 or ディレクトリ名)`
    * 指定したファイルまたはディレクトリを強制削除する
